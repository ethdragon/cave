language: node_js
node_js: 8
notifications:  
  email: false
branches:
  only:
    - master
cache:
  - npm
  - directories:
    - "$HOME/.npm"
install:
  - npm install
  - npm -g install serverless
script:
  - npm test
  - npm run lint
after_success:
  - ./travis-deploy.sh
